export const generateOTPCode = () => {
  // should be 6 digits
  // return Math.floor(100000 + Math.random() * 999999);

  // TODO: Remove and replace when the OTP service is implemented
  return "123456";
};
